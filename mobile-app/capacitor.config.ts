import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clouds5.roadreport',
  appName: 'Cloud S5 Road Report',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
