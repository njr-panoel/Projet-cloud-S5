// Legacy entrypoint (removed)
// This project uses Vite + Ionic.
// If you see an error referencing this file, stop any running 'ionic serve' that uses Vue CLI and run the Vite dev-server instead:
//   npm run dev  # or npm run start (ionic serve with Vite)

console.warn('Legacy main.js present to support older build tools. Use Vite (npm run dev) instead.');

